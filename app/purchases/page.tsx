import { PurchaseList } from "@/features/purchases";

export const metadata = {
    title: "Purchase Ledger | Inventory Management System",
};

export default function PurchasesPage() {
    return <PurchaseList />;
}