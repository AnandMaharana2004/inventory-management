import { VendorList } from "@/features/vendors";

export const metadata = {
  title: "Vendor Registry | Inventory Management System",
};

export default function VendorsPage() {
  return <VendorList />;
}